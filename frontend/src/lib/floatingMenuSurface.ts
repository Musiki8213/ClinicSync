/**
 * Shared styles for portaled floating menus app-wide (Select, Popover, DropdownMenu).
 * Solid `bg-background`, high `z-50`, subtle border + shadow + ring for separation.
 */
export const floatingMenuSurface =
  'z-50 rounded-xl border border-border bg-background text-foreground shadow-md shadow-black/[0.06] ring-1 ring-border/50 outline-none data-[state=open]:animate-in dark:shadow-black/25 dark:ring-border/60'
